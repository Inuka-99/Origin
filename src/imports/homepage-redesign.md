Using the existing ORIGIN Dashboard as the base, redesign the homepage to be more action-focused and personalized, similar to modern productivity tools like ClickUp.

Do not change the brand styling. Maintain the same color palette, typography, spacing system (8px), border radius, and clean enterprise SaaS design. No gradients.

Make the following updates:

Remove the fixed “Workload Distribution” chart from the main homepage layout.

Add a personalized greeting section at the top of the page:

Large heading: “Hey Sarah, what are we working on today?”

Smaller subtext: “Here’s a quick overview of your tasks and progress.”

Keep tone professional but friendly.

Use dynamic user name placeholder.

Add a “Today’s Focus” section:

A clean card showing:

Tasks Due Today (list format, 3–5 sample items)

Each task shows title, project tag, and due time

Small checkbox icon on left

Button: “View All Tasks”

Add a “Recently Completed” section:

List 3 recently completed tasks

Show task title, project tag, and completion timestamp

Slightly muted styling to show they’re completed

Add a “Customize Dashboard” or “Add Widget” button near the top right:

When clicked, users can choose widgets like:

Workload Distribution (chart)

Activity Feed

Progress Overview

Calendar Snapshot

Do not design the full modal yet — just show button and placeholder.

Keep the existing metric summary cards (Tasks Due, Overdue, Active Projects, Completion Rate), but make them secondary to the action-focused task list.

Ensure layout hierarchy:

Greeting at top

Focus tasks next

Completed section

Metrics below

Optional widgets last

Maintain consistent spacing and alignment.

Keep sidebar and top navigation unchanged.

Align all new elements cleanly within the dashboard container.